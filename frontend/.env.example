# GovProcu Frontend 환경변수 템플릿
# .env.local 로 복사해서 사용

# Anthropic Claude API (자연어 콘솔용)
ANTHROPIC_API_KEY=

# GovProcu MCP 서버 URL (uvicorn app.server:app --port 8080 기본)
GOVPROCU_MCP_URL=http://localhost:8080
